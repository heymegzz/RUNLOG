import { Router } from 'express';
// import authenticate from '../middleware/authenticate.js';
// import requireWorkspace from '../middleware/requireWorkspace.js';
// import requireRole from '../middleware/requireRole.js';

const router = Router();

// router.get('/', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), listJobs);
// router.post('/', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), createJob);
// router.get('/:id', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), getJob);
// router.patch('/:id', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), updateJob);
// router.delete('/:id', authenticate, requireWorkspace, requireRole(['owner','admin']), deleteJob);
// router.post('/:id/pause', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), pauseJob);
// router.post('/:id/resume', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), resumeJob);
// router.post('/:id/trigger', authenticate, requireWorkspace, requireRole(['owner','admin','developer']), triggerJob);

export default router;
