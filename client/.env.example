# Local dev: Vite proxies /api to the backend (see vite.config.js)
# Leave unset to use the proxy default (/api → http://localhost:5005)

# Only set this if the API is on a different origin (e.g. production API URL):
# VITE_API_URL=https://api.yourdomain.com/api
