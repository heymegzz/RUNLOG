// Layout component placeholder — sidebar + header + content area
// Implement in Phase 5
const Layout = ({ children }) => {
  return (
    <div className="layout">
      {/* Sidebar */}
      {/* Header */}
      <main>{children}</main>
    </div>
  );
};
export default Layout;
